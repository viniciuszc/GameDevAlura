class Enemy extends Animate{
    constructor(arraySprite, image, x, y, width, heigth, spriteWidh, spriteHeight) {
        super(arraySprite, image, x, y, width, heigth, spriteWidh, spriteHeight);
    }

}